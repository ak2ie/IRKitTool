<Window x:Class="IRKitTool.Views.AddIRCommand"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True" Height="230">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="30" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <!-- タイトル -->
        <TextBlock FontSize="15" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">赤外線コマンド登録</TextBlock>
        <!-- 名称 -->
        <TextBlock Grid.Row="1" Grid.Column="0">名称：</TextBlock>
        <TextBox Grid.Row="1" Grid.Column="1"></TextBox>
        <!-- コマンド -->
        <TextBlock Grid.Row="2" Grid.Column="0">コマンド：</TextBlock>
        <TextBlock Grid.Row="2" Grid.Column="1">未登録</TextBlock>
        <Button Grid.Row="2" Grid.Column="1" Width="80" Height="30">登録</Button>
        <!-- 登録ボタン -->
        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Width="300" Height="30">登録</Button>
    </Grid>
</Window>
